# QuizVerse — Envato delivery package

This folder contains buyer-facing material for the QuizVerse Envato item.

- `index.html` — standalone installation and product documentation.
- `description.html` — copy-ready Envato item description.
- `SCREENSHOTS.md` — required real-screen capture list and exact export dimensions.

The documentation describes only functionality present in the included Flutter app and Laravel admin/API codebase. No borrowed template visuals or fabricated product screenshots are included.

Open `index.html` in any modern browser. It has no build step and does not load external assets.
